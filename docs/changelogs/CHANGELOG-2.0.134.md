# Creator Control Suite 2.0.134

## Three-zone dashboard compile completion
- Qualified WPF Mouse access through System.Windows.Input.Mouse.
- Added the missing reverse dashboard module resolver GetDashboardModuleKey(FrameworkElement).
- The resolver maps live dashboard elements back to their persisted module keys.
- Fixes zone persistence during drag/drop and visual-tree order saving.
- Preserves the three-zone layout, hide/restore behavior, context menus and ScrollViewer structure fixes.
