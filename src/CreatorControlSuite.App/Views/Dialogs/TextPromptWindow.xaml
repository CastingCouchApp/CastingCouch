<Window x:Class="CreatorControlSuite.App.Views.Dialogs.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Eingabe"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" FontSize="20" FontWeight="SemiBold"/>
        <TextBlock x:Name="PromptText" Grid.Row="1" Foreground="{DynamicResource TextMutedBrush}" TextWrapping="Wrap" Margin="0,8,0,12"/>
        <TextBox x:Name="ValueBox" Grid.Row="2" MinHeight="34"/>
        <TextBlock x:Name="StatusText" Grid.Row="3" Foreground="{DynamicResource AccentHoverBrush}" TextWrapping="Wrap" Margin="0,8,0,0" MinHeight="18"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CancelButton" Content="Abbrechen" MinWidth="100" Margin="0,0,8,0" IsCancel="True"/>
            <Button x:Name="OkButton" Content="OK" MinWidth="100" IsDefault="True"
                    Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource TextOnAccentBrush}" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
