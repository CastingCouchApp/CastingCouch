<Window x:Class="CreatorControlSuite.App.Views.Dialogs.FirstRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Creator Control Suite – Ersteinrichtung"
        Width="900"
        Height="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Creator Control Suite"
                       FontSize="30"
                       FontWeight="Bold"/>
            <TextBlock x:Name="StepTitleText"
                       Text="Willkommen"
                       Foreground="{DynamicResource AccentBrush}"
                       FontSize="18"
                       Margin="0,6,0,0"/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="22"
                Margin="0,20,0,18">
            <Grid>
                <Grid x:Name="WelcomeStep">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Willkommen bei der Creator Control Suite"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   TextAlignment="Center"/>
                        <TextBlock Text="Dieser Assistent richtet die Grundwerte ein. Twitch und Spotify werden anschließend in der Suite autorisiert."
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   FontSize="17"
                                   Margin="40,18,40,0"/>
                    </StackPanel>
                </Grid>

                <Grid x:Name="BrandingStep" Visibility="Collapsed">
                    <StackPanel Width="620">
                        <TextBlock Text="Name und Kanal"
                                   Foreground="{DynamicResource AccentBrush}"
                                   FontSize="22"
                                   FontWeight="Bold"/>
                        <TextBlock Text="Anzeigename" Margin="0,14,0,4"/>
                        <TextBox x:Name="DisplayNameBox"/>
                        <TextBlock Text="Twitch-Kanalname" Margin="0,14,0,4"/>
                        <TextBox x:Name="ChannelNameBox"/>
                    </StackPanel>
                </Grid>

                <Grid x:Name="ObsStep" Visibility="Collapsed">
                    <StackPanel Width="620">
                        <TextBlock Text="OBS"
                                   Foreground="{DynamicResource AccentBrush}"
                                   FontSize="22"
                                   FontWeight="Bold"/>
                        <Grid Margin="0,14,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="160"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Host"/>
                                <TextBox x:Name="ObsHostBox"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                <TextBlock Text="Port"/>
                                <TextBox x:Name="ObsPortBox"/>
                            </StackPanel>
                        </Grid>
                        <TextBlock Text="Startszene" Margin="0,10,0,4"/>
                        <TextBox x:Name="StartSceneBox"/>
                        <TextBlock Text="Live-Szene" Margin="0,10,0,4"/>
                        <TextBox x:Name="LiveSceneBox"/>
                        <TextBlock Text="Pausenszene" Margin="0,10,0,4"/>
                        <TextBox x:Name="PauseSceneBox"/>
                        <TextBlock Text="Endszene" Margin="0,10,0,4"/>
                        <TextBox x:Name="EndSceneBox"/>
                    </StackPanel>
                </Grid>

                <Grid x:Name="OverlayStep" Visibility="Collapsed">
                    <StackPanel Width="620">
                        <TextBlock Text="Overlay"
                                   Foreground="{DynamicResource AccentBrush}"
                                   FontSize="22"
                                   FontWeight="Bold"/>
                        <TextBlock Text="Canvas und Widgets laufen über den lokalen Overlay-Webserver (kein Ordner nötig)."
                                   TextWrapping="Wrap"
                                   Margin="0,14,0,8"/>
                        <TextBlock Text="Optionaler Datenordner (leer = LocalAppData)" Margin="0,8,0,4"/>
                        <TextBox x:Name="OverlayRootBox"/>
                        <CheckBox x:Name="InstallBundledOverlayBox"
                                  Content="Overlay-Daten vorbereiten"
                                  IsChecked="True"
                                  Margin="0,12,0,0"/>
                        <TextBlock Text="OBS nutzt /view/default (Canvas) oder /w/… (Einzel-Widgets)."
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   TextWrapping="Wrap"
                                   Margin="0,12,0,0"/>
                    </StackPanel>
                </Grid>

                <Grid x:Name="SummaryStep" Visibility="Collapsed">
                    <StackPanel Width="680">
                        <TextBlock Text="Zusammenfassung"
                                   Foreground="{DynamicResource AccentBrush}"
                                   FontSize="22"
                                   FontWeight="Bold"/>
                        <TextBlock x:Name="SummaryText"
                                   TextWrapping="Wrap"
                                   FontSize="17"
                                   Margin="0,14,0,0"/>
                        <CheckBox x:Name="OpenSettingsAfterWizardBox"
                                  Content="Nach Abschluss die Einstellungen öffnen"
                                  IsChecked="True"
                                  Margin="0,18,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelButton"
                    Content="Abbrechen"
                    Padding="14,9"/>

            <WrapPanel Grid.Column="1">
                <Button x:Name="BackButton"
                        Content="Zurück"
                        IsEnabled="False"
                        Padding="14,9"
                        Margin="4"/>
                <Button x:Name="NextButton"
                        Content="Weiter"
                        Background="{DynamicResource AccentBrush}"
                        Foreground="{DynamicResource TextOnAccentBrush}"
                        FontWeight="Bold"
                        Padding="14,9"
                        Margin="4"/>
            </WrapPanel>
        </Grid>
    </Grid>
</Window>
