<Window x:Class="CreatorControlSuite.App.Views.Dialogs.AssetLibraryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Asset-Bibliothek"
        Width="720"
        Height="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,12">
            <Button x:Name="ImportButton"
                    Content="Hochladen…"
                    DockPanel.Dock="Right"
                    MinWidth="110"
                    Padding="12,6"/>
            <TextBlock Text="Bilder aus der Bibliothek auswählen oder hochladen."
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextMutedBrush}"
                       TextWrapping="Wrap"/>
        </DockPanel>
        <ListBox x:Name="AssetList"
                 Grid.Row="1"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 BorderBrush="{DynamicResource BorderBrush}"
                 BorderThickness="1">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Width="140" Margin="6" Padding="6"
                            Background="{DynamicResource SurfaceBrush}"
                            CornerRadius="6">
                        <StackPanel>
                            <Image Source="{Binding Preview}"
                                   Height="100"
                                   Stretch="Uniform"/>
                            <TextBlock Text="{Binding Name}"
                                       Margin="0,6,0,0"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button x:Name="DeleteButton"
                    Content="Löschen"
                    MinWidth="100"
                    Margin="0,0,8,0"/>
            <Button x:Name="CancelButton"
                    Content="Abbrechen"
                    MinWidth="100"
                    Margin="0,0,8,0"
                    IsCancel="True"/>
            <Button x:Name="SelectButton"
                    Content="Auswählen"
                    MinWidth="100"
                    IsDefault="True"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="{DynamicResource TextOnAccentBrush}"
                    FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
