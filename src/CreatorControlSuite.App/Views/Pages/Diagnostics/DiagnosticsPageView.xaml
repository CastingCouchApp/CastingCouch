<UserControl x:Class="CreatorControlSuite.App.Views.Pages.Diagnostics.DiagnosticsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,8">
            <TextBlock Text="{Binding Title}"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       VerticalAlignment="Center"/>
            <Button DockPanel.Dock="Right"
                    Content="AKTUALISIEREN"
                    Command="{Binding RefreshCommand}"
                    Padding="12,4"
                    Margin="12,0,0,0"/>
        </DockPanel>

        <TextBlock Grid.Row="1"
                   Text="{Binding StatusMessage}"
                   Foreground="{DynamicResource MutedBrush}"
                   Margin="0,0,0,8"
                   TextWrapping="Wrap"/>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding ModuleStatuses}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  Background="Transparent"
                  BorderThickness="0"
                  RowBackground="Transparent"
                  AlternatingRowBackground="{DynamicResource ElevatedBackgroundBrush}"
                  Foreground="{DynamicResource TextPrimaryBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Modul" Binding="{Binding DisplayName}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Health}" Width="100"/>
                <DataGridTextColumn Header="Detail" Binding="{Binding Detail}" Width="2*"/>
                <DataGridTextColumn Header="Geprüft" Binding="{Binding CheckedAt, StringFormat={}{0:HH:mm:ss}}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
