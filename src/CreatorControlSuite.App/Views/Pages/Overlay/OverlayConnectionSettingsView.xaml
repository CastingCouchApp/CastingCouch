<UserControl x:Class="CreatorControlSuite.App.Views.Pages.Overlay.OverlayConnectionSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <Border Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="9"
                Padding="14"
                Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Overlay-Webserver"
                           Foreground="{DynamicResource AccentBrush}"
                           FontWeight="Bold"/>
                <TextBlock Text="OBS-Browserquellen nutzen http://127.0.0.1:&lt;Port&gt;/o/&lt;id&gt;/… Live-Events über ws://127.0.0.1:&lt;Port&gt;/ws."
                           Foreground="{DynamicResource MutedBrush}"
                           TextWrapping="Wrap"
                           Margin="0,4,0,10"/>
                <CheckBox Content="Webserver aktivieren"
                          IsChecked="{Binding WebServerEnabled}"
                          Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Port"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1"
                             Text="{Binding WebServerPort}"/>
                    <TextBox Grid.Column="2"
                             Text="{Binding BaseUrl}"
                             IsReadOnly="True"
                             Margin="12,0,0,0"/>
                    <Button Grid.Column="3"
                            Content="WS-BASE KOPIEREN"
                            Command="{Binding CopyBaseUrlCommand}"
                            Margin="8,0,0,0"/>
                </Grid>
                <TextBlock Text="{Binding ServerStatus}"
                           Foreground="{DynamicResource MutedBrush}"
                           Margin="0,8,0,0"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="9"
                Padding="14"
                Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Chat-Overlay"
                           Foreground="{DynamicResource AccentBrush}"
                           FontWeight="Bold"/>
                <TextBlock Text="Eingebaute Seite unter /chat. Nachrichten inkl. Emotes kommen über denselben WebSocket (/ws)."
                           Foreground="{DynamicResource MutedBrush}"
                           TextWrapping="Wrap"
                           Margin="0,4,0,10"/>
                <CheckBox Content="Chat-Overlay aktiv"
                          IsChecked="{Binding ChatEnabled}"
                          Margin="0,0,0,8"/>
                <CheckBox Content="Twitch-Events einblenden (Follow, Sub, Raid, …)"
                          IsChecked="{Binding ShowTwitchEvents}"
                          Margin="0,0,0,8"/>
                <CheckBox Content="BTTV-Emotes"
                          IsChecked="{Binding EnableBttv}"
                          Margin="0,0,0,4"/>
                <CheckBox Content="FrankerFaceZ-Emotes"
                          IsChecked="{Binding EnableFfz}"
                          Margin="0,0,0,4"/>
                <CheckBox Content="7TV-Emotes"
                          IsChecked="{Binding EnableSevenTv}"
                          Margin="0,0,0,12"/>

                <TextBlock Text="Darstellung"
                           Foreground="{DynamicResource AccentBrush}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6"/>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Hintergrund"
                               VerticalAlignment="Center"/>
                    <ComboBox Grid.Column="1"
                              Margin="0,0,0,6"
                              SelectedValuePath="Tag"
                              SelectedValue="{Binding BackgroundType}">
                        <ComboBoxItem Content="Keiner (transparent)"
                                      Tag="None"/>
                        <ComboBoxItem Content="Farbe"
                                      Tag="Color"/>
                        <ComboBoxItem Content="Bild"
                                      Tag="Image"/>
                    </ComboBox>
                    <TextBlock Text="Farbe"
                               Grid.Row="1"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1"
                             Grid.Column="1"
                             Text="{Binding BackgroundColor}"
                             Margin="0,0,0,6"
                             ToolTip="#RRGGBB"/>
                    <TextBlock Text="Bild"
                               Grid.Row="2"
                               VerticalAlignment="Center"/>
                    <DockPanel Grid.Row="2"
                               Grid.Column="1"
                               Margin="0,0,0,6">
                        <Button Content="Durchsuchen…"
                                Command="{Binding BrowseBackgroundCommand}"
                                DockPanel.Dock="Right"
                                Margin="8,0,0,0"/>
                        <TextBox Text="{Binding BackgroundImagePath}"/>
                    </DockPanel>
                    <TextBlock Text="Transparenz %"
                               Grid.Row="3"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="3"
                             Grid.Column="1"
                             Text="{Binding BackgroundOpacityPercent}"
                             Margin="0,0,0,6"
                             ToolTip="0–100, nur Hintergrund"/>
                    <TextBlock Text="Padding px"
                               Grid.Row="4"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="4"
                             Grid.Column="1"
                             Text="{Binding PaddingPx}"
                             Margin="0,0,0,6"/>
                    <TextBlock Text="Eckenradius px"
                               Grid.Row="5"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="5"
                             Grid.Column="1"
                             Text="{Binding BorderRadiusPx}"
                             Margin="0,0,0,6"/>
                    <TextBlock Text="Abstand px"
                               Grid.Row="6"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="6"
                             Grid.Column="1"
                             Text="{Binding GapPx}"
                             Margin="0,0,0,6"/>
                    <TextBlock Text="Schriftgröße px"
                               Grid.Row="7"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="7"
                             Grid.Column="1"
                             Text="{Binding FontSizePx}"
                             Margin="0,0,0,6"
                             ToolTip="8–72"/>
                    <TextBlock Text="Schriftart"
                               Grid.Row="8"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="8"
                             Grid.Column="1"
                             Text="{Binding FontFamily}"
                             ToolTip="z. B. Segoe UI, Arial, Impact"/>
                </Grid>

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding ChatUrl}"
                             IsReadOnly="True"/>
                    <Button Grid.Column="1"
                            Content="URL KOPIEREN"
                            Command="{Binding CopyChatUrlCommand}"
                            Margin="8,0,0,0"/>
                </Grid>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
