<UserControl x:Class="CreatorControlSuite.App.Views.Pages.Services.Parts.CreatorIntelligencePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource ElevatedBackgroundBrush}"
            BorderBrush="{DynamicResource SubtleBorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Padding="14">
        <StackPanel>
            <TextBlock Text="Creator Intelligence" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" Margin="0,8,0,0"/>
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <TextBlock Text="Creator Score" VerticalAlignment="Center" Foreground="{DynamicResource MutedBrush}"/>
                <TextBlock Text="{Binding ScoreText}" FontSize="28" FontWeight="Bold" Margin="12,0,0,0"
                           Foreground="{DynamicResource AccentBrush}"/>
            </StackPanel>
            <WrapPanel Margin="0,12,0,0">
                <Button Content="Aktualisieren" Command="{Binding RefreshCommand}" Margin="0,0,8,8"/>
                <Button Content="Ordner öffnen" Command="{Binding OpenFolderCommand}" Margin="0,0,8,8"/>
                <Button Content="Wochenbericht" Command="{Binding WeeklyReportCommand}" Margin="0,0,8,8"/>
            </WrapPanel>
        </StackPanel>
    </Border>
</UserControl>
