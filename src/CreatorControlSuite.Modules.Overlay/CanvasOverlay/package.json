{
  "name": "ccs-canvas-overlay",
  "private": true,
  "version": "1.0.0",
  "description": "Creator Control Suite Canvas Overlay (TypeScript)",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
